import { NavigationPlaceholder } from '@/components/navigation-placeholder';

export default function HelpFeedbackScreen() {
  return (
    <NavigationPlaceholder
      title="Help / Feedback"
      description="Help and feedback will be implemented here once a support destination is configured. No feedback or diagnostics are sent."
    />
  );
}
