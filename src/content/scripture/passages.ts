import type { BibleBook, ScripturePassage } from '@/scripture/types';

// Add only books/ranges needed by the approved plan, with verified versification sources.
export const bibleBooks: readonly BibleBook[] = [];
export const scripturePassages: readonly ScripturePassage[] = [];
