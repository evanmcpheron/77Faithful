import styled from 'styled-components/native';

import { Spacing } from '@td/theme/spacing';

export const StyledLoadingState = styled.View({
	alignItems: 'center',
	gap: Spacing.XSmall,
	justifyContent: 'center',
	padding: Spacing.Small,
});
