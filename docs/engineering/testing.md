# Testing and verification

Updated 2026-09-07. The repository has an operational lint/format/type/test setup and a small regression suite. Navigation-scaffold tests now exercise parser and route relationships; integrated product/backend tests are still future work.

## Tooling and conventions

- Node 24 and npm 11; `.nvmrc` and package engines define the supported development runtime. Use `npm ci` and commit lockfile changes with dependency changes.
- ESLint uses `eslint-config-expo/flat` plus `eslint-config-prettier`, which disables conflicting formatting rules. Prettier runs separately. Explicit `any`, incorrect type imports, and lint warnings fail verification.
- Jest 29 uses `jest-expo ~57.0.5` and the React Native 0.86.3 preset. React Native Testing Library 14 uses its `test-renderer` peer. Await `render`, `renderHook`, `act`, and user interactions; prefer accessible role/name queries and behavioral assertions.
- Tests are colocated as `src/**/*.test.tsx` or `*.test.ts`, outside `src/app/`. There are no snapshots or arbitrary coverage thresholds. Coverage collection includes untested source rather than just imported modules.
- `jest.setup.js` uses the official Worklets mock before Reanimated's test setup, and mocks the global CSS import. Keep real components/hooks under test; isolate native/platform or external-service boundaries. [Worklets setup](https://docs.swmansion.com/react-native-worklets/docs/guides/testing/), [Reanimated setup](https://docs.swmansion.com/react-native-reanimated/docs/guides/testing/).
- Jest disables Watchman for portable local/CI runs. Mock call history and spies are reset between tests; configure individual mock return values in each suite.

ESLint 9 is currently required by the peer range of `eslint-plugin-react` used by Expo's SDK 57 configuration. npm marks this ESLint major unsupported. Keep this constraint visible and move to a supported major when the Expo/plugin combination supports it; do not force incompatible peer dependencies. Jest 29 likewise follows this version of `jest-expo` rather than an independent latest-major upgrade.

## Commands

| Command                                                          | Purpose                                                                                                        |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `npm run check`                                                  | Formatting, lint, generated route types and TypeScript, then CI-mode tests                                     |
| `npm run typegen`                                                | `expo customize tsconfig.json`; regenerates ignored Expo environment/route declarations without starting Metro |
| `npm run typecheck`                                              | Type generation followed by `tsc --noEmit`                                                                     |
| `npm run lint` / `npm run lint:fix`                              | Check the repository / apply available lint fixes                                                              |
| `npm run format:check` / `npm run format`                        | Check formatting / format supported repository files                                                           |
| `npm test` / `npm run test:watch`                                | Run tests once / watch tests                                                                                   |
| `npm run test:ci`                                                | Run Jest once, serially, in CI mode                                                                            |
| `npm test -- --runInBand src/components/ui/collapsible.test.tsx` | Target one suite                                                                                               |
| `npm run test:coverage`                                          | Report coverage and write ignored output to `coverage/`                                                        |
| `npm run export:web`                                             | Build the static web preview in ignored `dist/`                                                                |
| `git diff --check`                                               | Check diff whitespace only                                                                                     |

The committed TypeScript configuration explicitly loads `expo/types`, so CSS imports type-check even before Expo's first run. The normal `typecheck` command also generates route-specific declarations, avoiding a misleading pass without route validation. Keep `.expo/` and `expo-env.d.ts` ignored and do not hand-edit them. [Expo typed routes](https://docs.expo.dev/router/reference/typed-routes/).

GitHub Actions runs `npm ci`, `npm run check`, and `npm run export:web` for pull requests and pushes to `main`, using `.nvmrc`, read-only repository permissions, and no application secrets. It does not build signed native binaries or deploy anything.

## Check scope

For Markdown-only work, run `npm run format:check`, resolve relative links and any heading fragments in changed documents, and check new or changed external references with available tools. Use `git diff --check` for whitespace; it does not validate links or include untracked files. Format only affected files with the installed Prettier CLI rather than applying unrelated repository-wide fixes. No additional link-checking package is required. Report external references that could not be verified.

For reviews, follow the non-editing rules in [AGENTS.md](../../AGENTS.md). Use check commands rather than `format` or `lint:fix`. The type generation behind `typecheck` and `check` invokes Expo customization, which can update `tsconfig.json` if setup is incomplete; inspect configuration before using it in a review. Ignored type/build outputs are acceptable, but rewriting source/configuration is not part of a review.

Root Jest currently discovers only `src/**/*.test.[jt]s?(x)`. A passing root check will not establish coverage of the planned separate `functions/` package or Firestore rules. When adding those integrations, supply appropriate backend/emulator test commands, include applicable checks in CI, and document their setup and runtime requirements here. These tools are not yet configured; do not scaffold them for unrelated work.

## Regression coverage

| Location                                 | Behavior covered                                                                                                                                                                                                         |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/components/themed-view.test.tsx`    | Light/dark overrides, unspecified-scheme fallback, palette fallback, and caller style precedence                                                                                                                         |
| `src/components/themed-text.test.tsx`    | Theme-aware primary link colors and explicit palette overrides                                                                                                                                                           |
| `src/components/ui/collapsible.test.tsx` | Accessible name/role, expanded/collapsed state, and content toggling through user presses                                                                                                                                |
| `src/navigation/day-number.test.ts`      | Every valid day plus malformed, noncanonical, array, and out-of-range parameters                                                                                                                                         |
| `src/navigation/navigation.test.tsx`     | Auth/onboarding push/back, two destinations and web current-page state, shared 48-point controls, Settings child/source returns, day boundaries/query collisions/invalid redirects, completion review, static parameters |
| `src/hooks/use-color-scheme.test.tsx`    | Web appearance subscription, updates, and cleanup                                                                                                                                                                        |

Historical validation during engineering setup on 2026-09-07: a clean `npm ci`, `npm run check` (including all ten tests without snapshots), and `npm run export:web` passed. The source no longer has the original missing-CSS type errors or ignored themed-view overrides. Browser startup failed in that automation connection, and no native-device checks or remote CI run were observed. These results describe that checkout and environment; they are not evidence for later changes or a reason to skip newly available runtime checks.

Navigation-foundation audit on 2026-09-07: `npm test -- --runInBand src/navigation` passed all 69 navigation cases. `npm run check` passed formatting, lint, generated route types, strict TypeScript, and all 79 tests across six suites. `npm run export:web` passed with all 23 required URL patterns and exactly 231 concrete public day pages, each containing the correct day heading; Router also generates group aliases and dynamic fallback files. No duplicate or excluded product routes were found. An additional `expo export --platform ios` to temporary output passed native JavaScript/Hermes bundling. Regression checks reproduced the missing web current-page attribute and the loss of shared controls' 48-point styles through Router's Link slot before the fixes. Router tests load the actual route directory, substitute web tabs for native tabs, and omit the native splash overlay; they exercise JavaScript navigation state, not native gestures or authentication.

Runtime audit used isolated headless Chrome against the local static export after the in-app browser connection failed during setup. Auth and onboarding push/back, Today/Journey switching, Settings child/source returns, Account/Delete Account Back and Cancel, day/Scripture/Reflection returns, completion review, malformed day redirects, and repeated query parameters were exercised. Web checks verified current-page semantics, keyboard Tab/Shift+Tab/Enter and visible focus, the readable Welcome Back label, 48-point navigation targets, and scrolling in light/dark themes at a 320-pixel viewport with 200% CSS zoom. These are browser checks, not native text-scaling or screen-reader verification. An iPhone 17 Pro simulator on iOS 26.3 with Expo Go 57.0.9 loaded Today, establishing native startup only. Native UI automation was unavailable, so iOS Back/swipe gestures, VoiceOver, and native text scaling remain unverified. Android SDK/device tooling was unavailable; Android system Back and TalkBack were not tested. Auth/onboarding guards, persistence, and journey-dependent access remain deferred integrations.

## Navigation and user-flow coverage

[docs/APP_NAVIGATION_AND_UX.md](../APP_NAVIGATION_AND_UX.md) owns expected routes, screen relationships, redirects, day states, and V1/Future behavior. Read the applicable screen/state rules before choosing assertions. Test meaningful flow outcomes, not just rendering or whether a router mock was called. The cases below apply as their features are implemented or changed; they do not require speculative suites, a new test framework, or tests for absent Community/optional features.

| Area                          | Cases to cover when implemented and in scope                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Authentication                | Logged-out launch; unresolved session restoration without auth flash; authenticated launch with onboarding incomplete/complete; protected direct routes; sign-out clears protected history; failed profile loads do not invent a journey or force sign-out. Check selected verified-write authorization and the contract's unresolved email-gate placement separately.                                                                    |
| Onboarding                    | Required step order; exactly two distinct optional practices; supported Bible translation selection and failure/retry; persisted selections and resume at the first incomplete step; idempotent start; successful completion redirect only after coherent persistence; Back cannot reopen completed onboarding.                                                                                                                           |
| Current day                   | Zero, some, and all five practices complete; Scripture and Reflection entry/return; Continue targets the expected incomplete practice; all five are required for complete-day status, intention is excluded; explicit completion is reversible; typing/draft autosave and reader activity do not auto-complete; reopening a complete day remains possible without advancing tomorrow.                                                     |
| Journey/history               | Previous incomplete and completed days; current-day row goes to the canonical current screen; future rows expose understandable locked state and reject direct content/edit access; invalid day parameters; calendar/DST/time-zone boundaries; Day 77 persistence and completion entry; ended-journey Today and historical access where implemented. Resolve the contract's ended-Day-77 guard ambiguity before asserting that edge case. |
| Missed days                   | A missed/partial day never restarts, extends, or blocks the journey; incomplete history remains visible and reviewable; the current day remains accessible; historical edits do not alter calendar progression.                                                                                                                                                                                                                           |
| Settings                      | Practice and Bible translation changes, validation, persistence/reload, save failure, documented effect timing and historical integrity; child screens return correctly; Settings returns to the source tab; cancellation/unsaved-change handling.                                                                                                                                                                                        |
| Back/dismissal                | iOS header back and swipe-back; Android hardware/system Back; tab roots; modal/sheet dismissal; safe draft retention or unsaved-change guard; no return to auth/onboarding after their gates complete, or protected content after sign-out.                                                                                                                                                                                               |
| Scripture failure and privacy | API.Bible loading/error/retry leaves known references and unrelated practices available and never completes Scripture; external-reading completion requires explicit action; translation changes do not corrupt progress. Private reflections, journals, intentions, and prayer content stay out of URLs, logs, diagnostics, and implicit sharing.                                                                                        |
| Deep links / Community        | Test destination preservation through auth/onboarding and destination validation only when links are supported. V1 must expose no Community entry points. Membership, revocation, invitation, and explicit sharing cases belong with an explicitly enabled future feature.                                                                                                                                                                |

Use pure domain/service tests for calculations, validation, and persistence outcomes; interaction/integration tests for CTA destinations, guards, and recovery; and available iOS/Android runtimes for actual back gestures, focus, keyboard, and dismissal. Use synthetic data and deterministic clocks. A mocked route trigger or static web export cannot establish native navigation behavior.

Keep the contract's open decisions visible in test plans. Use a documented recommended V1 rule where it resolves the expectation; report conflicting or unspecified expectations rather than permanently encoding an arbitrary policy or accepting a defect to obtain a pass. Material behavior changes require the same-change contract update under [AGENTS.md](../../AGENTS.md).

## Remaining coverage

Add regression cases when fixing bugs and practical behavior tests with each domain/integration feature. In particular:

- Router navigation, native splash lifecycle, external links, safe areas, larger text, contrast, reduced motion, and future keyboard/forms need platform verification.
- The chosen calendar/time-zone rules need date-boundary, daylight-saving, travel, missed-day, and historical-edit tests when implemented. Use deterministic clocks and synthetic records.
- Firebase integration tests need allowed operations and unauthenticated/unverified/cross-user denial, invalid or protected-field writes, and privileged-handler authorization. Add membership/revocation cases only with scoped community access. Use emulators for enforcement; verify offline/pending data, account switching/deletion, and journal conflicts at the appropriate service/native boundary.
- Future API.Bible tests must cover input validation, explicit translation selection, response parsing, attribution, timeouts, quotas, and unavailable Scripture states without calling the live provider in routine tests.

Follow [AGENTS.md](../../AGENTS.md) for scope and test quality. Report the exact checks run and their limits; never imply that mocked tests establish native runtime behavior or production security.
